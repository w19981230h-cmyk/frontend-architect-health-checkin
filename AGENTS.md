# 项目协作规范

## 沟通方式

- 涉及代码或开发知识时，使用产品视角的语言向用户说明，优先描述用户价值、界面行为和验收结果，避免只罗列技术实现。

## 通用组件规范

### 列表与查询组件

- 以后凡涉及界面设计、界面调整或前端实现，只要包含列表、表格、搜索、筛选、查询条件、查询按钮、重置按钮或分页，开始设计和实现前都必须先直接读取用户指定的 Ant Design 官方文档入口：<https://ant.design/components/button-cn>。
- 以上链接是本项目列表与查询界面的指定规范入口。即使入口当前落在 Button 文档页，也只能继续采用同一 Ant Design 官方组件体系中已有的列表、表格、表单、输入、选择、日期、按钮、间距、分页及其组合规范。
- 禁止自行新增另一套查询栏、筛选面板、列表、表格、分页、按钮变体或视觉风格；禁止为了“更好看”而偏离 Ant Design 的现成结构、交互状态、尺寸层级和设计 Token。
- 优先复用项目中已有且符合 Ant Design 规范的通用组件。确需扩展时，只允许做业务字段、数据状态、权限和响应式适配等产品能力扩展，不创造新的查询或列表视觉范式。
- 开始实现前应检查官方文档的当前内容和版本，不依赖记忆中的旧版规则。交付前检查默认、悬停、禁用、加载、空数据、错误、分页和响应式状态是否仍符合官方规范。
- 如果用户的当次明确要求与本规范冲突，先向用户指出冲突并取得确认，不得静默改用自定义样式。

### 统计图表

- 以后凡涉及统计数据可视化、趋势分析、占比分析、分布对比、排行或其他图表显示，开始设计和实现前都必须先直接读取用户指定的 Apache ECharts 官方示例入口：<https://echarts.apache.org/examples/zh/index.html#chart-type-line>。
- 以上链接是本项目统计图表的指定规范入口。即使入口当前定位在线图分类，其他柱状图、饼图、散点图、雷达图、仪表盘、地图或组合图也只能从同一 Apache ECharts 官方示例体系中选择合适方案。
- 禁止脱离 ECharts 官方示例自行创造另一套图表类型、配色、图例、坐标轴、提示框、数据缩放、交互状态或视觉风格；优先复用官方示例的结构和配置，再结合项目现有设计 Token 做必要适配。
- 图表选型必须服务于业务问题和阅读效率，不得仅为装饰而增加图表。确需扩展时，只允许调整业务数据、指标口径、权限、状态、响应式布局和无障碍说明，不创造新的统计图表视觉范式。
- 开始实现前应检查官方示例的当前内容和版本，不依赖记忆中的旧版配置。交付前检查加载、空数据、异常、极值、长标签、图例切换、悬停提示、窗口缩放和移动端状态。
- 如果用户的当次明确要求与本规范冲突，先向用户指出冲突并取得确认，不得静默改用其他图表库或自定义图表样式。
